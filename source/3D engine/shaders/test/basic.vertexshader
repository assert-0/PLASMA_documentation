#version 330 core

//location coresponds to the one in the
//glVertexAttribPointer() call
layout(location = 0) in vec3 vertexPos;

void main()
{
    gl_Position.xyz = vertexPos;
    gl_Position.w = 1.0;
}
